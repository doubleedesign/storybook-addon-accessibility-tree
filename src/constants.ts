export const ADDON_ID = 'accessibility-tree';
export const PANEL_ID = `${ADDON_ID}/panel`;
export const KEY = 'accessibility-tree';

export const EVENTS = {
	RESULT: `${ADDON_ID}/result`,
	REQUEST: `${ADDON_ID}/request`,
};
